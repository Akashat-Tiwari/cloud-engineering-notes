## For Basic Commands 

* redirecting to GfG : [Basic_Commands](https://www.geeksforgeeks.org/linux-unix/basic-linux-commands/)

* cd : to change between directories : first home directory then required one

* locate : locate the file from database : only give output if the file was uploaded in database, if not uploaded then run: sudo updatedb , after it it will uploaded to database

* press 'q' to quit when manual page is opne via (man cmd)

* can access all the directories and files (wget also) created in linux CLI in FileExplorer of the system

* ctrl + C (cancel current command) : This cancels the current command (Broken command) and returns you to normal prompt


